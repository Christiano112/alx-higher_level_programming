-- lists all names and score records where score greater than or equals 10
-- resulst should be ordered by descending order of score

SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
